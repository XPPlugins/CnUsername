plugins {
    id 'java'
}

group = 'me.xpyex'
version = '1.1.2'

repositories {
    mavenCentral()
}

dependencies {
}

// 添加Java版本约束
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21) // 强制使用Java 17
    }
    withSourcesJar()
}